# Sample05

An example of nginx cluster. In the front is a balancer node, in the back are 3 nginx + php-fpm servers.

## (Re)Build + Run

```
make
```

## Stop

```
docker-compose down
```
